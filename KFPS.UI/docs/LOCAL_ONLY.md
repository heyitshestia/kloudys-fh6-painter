# Local-only workspace

This conversion repository was initialized from the user-supplied source archive. It has no Git remote and is not configured to push anywhere. The included local Git hook refuses every push command as an additional safeguard.
